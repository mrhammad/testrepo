<UserControl x:Class="OrderWiseErp.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Border Style="{StaticResource CardBorderStyle}" MaxWidth="520">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding Localization[Settings.Language]}"
                           VerticalAlignment="Center" />
                <ComboBox Grid.Column="1"
                          Margin="8,0,0,8"
                          ItemsSource="{Binding AvailableLanguages}"
                          SelectedValuePath="Code"
                          DisplayMemberPath="DisplayName"
                          SelectedValue="{Binding SelectedLanguageCode, Mode=TwoWay}" />

                <TextBlock Grid.Row="1"
                           Grid.ColumnSpan="2"
                           Text="{Binding Localization[Settings.FormHeader]}"
                           FontWeight="SemiBold"
                           Margin="0,4,0,0" />

                <TextBlock Grid.Row="1"
                           Grid.ColumnSpan="2"
                           Foreground="{StaticResource MutedTextBrush}"
                           Text="{Binding Localization[Settings.Help]}"
                           TextWrapping="Wrap"
                           Margin="0,8,0,0" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
