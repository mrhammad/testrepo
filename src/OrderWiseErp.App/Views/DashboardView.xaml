<UserControl x:Class="OrderWiseErp.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12">
            <TextBlock Text="Core phase-1 counters and quick health checks for your order-wise workflow."
                       TextWrapping="Wrap"
                       Foreground="{StaticResource MutedTextBrush}" />
        </Border>

        <UniformGrid Grid.Row="1" Columns="3" Rows="1">
            <Border Margin="0,0,12,12"
                    Padding="14"
                    CornerRadius="8"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    Background="White">
                <StackPanel>
                    <TextBlock Text="Projects" Foreground="{StaticResource MutedTextBrush}" />
                    <TextBlock Text="{Binding ProjectsCount}" FontSize="24" FontWeight="SemiBold" Margin="0,6,0,0" />
                </StackPanel>
            </Border>

            <Border Margin="0,0,12,12"
                    Padding="14"
                    CornerRadius="8"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    Background="White">
                <StackPanel>
                    <TextBlock Text="Contacts" Foreground="{StaticResource MutedTextBrush}" />
                    <TextBlock Text="{Binding ContactsCount}" FontSize="24" FontWeight="SemiBold" Margin="0,6,0,0" />
                </StackPanel>
            </Border>

            <Border Margin="0,0,12,12"
                    Padding="14"
                    CornerRadius="8"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    Background="White">
                <StackPanel>
                    <TextBlock Text="Products" Foreground="{StaticResource MutedTextBrush}" />
                    <TextBlock Text="{Binding ProductsCount}" FontSize="24" FontWeight="SemiBold" Margin="0,6,0,0" />
                </StackPanel>
            </Border>
        </UniformGrid>
    </Grid>
</UserControl>
